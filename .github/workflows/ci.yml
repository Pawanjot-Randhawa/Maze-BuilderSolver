name: ci

on:
    pull_request:
      branches:
        - main
    push:
      branches:
        - main

jobs:
    maven:
        name: Maven build
        runs-on: ubuntu-latest
        steps:
            
            - name: Checkout Code
              uses: actions/checkout@v4
                        
            - name: Clean Verify Maven
              run: mvn clean verify